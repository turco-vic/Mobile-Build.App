import { colors } from './colors';
import { typography } from './typography';
import { spacing, borderRadius, shadows, elevation } from './spacing';
import { components } from './components';

export const theme = {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  elevation,
  components
};

export default theme;